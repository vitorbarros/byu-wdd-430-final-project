import {styled, Card as CardNext} from "@mui/material";

export const Card = styled(CardNext)`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  align-self: center;
  padding: 35px;
  height: 300px;
  box-shadow: none;
`